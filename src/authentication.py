"""This library contains all the functions needed to 
   set, update, and store authentication pin."""

import psycopg2

# Connect to the database
# Create tables
# Read from database (return a pandas dataframe)
# Write to database
